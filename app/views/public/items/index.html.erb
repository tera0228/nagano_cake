<!-- 顧客側商品一覧画面 -->
<h2>商品一覧</h2>

<% @items.each do |item| %>
<div>
  <%= link_to item_path(item.id) do %>
    <%= image_tag item.get_item_image(250, 200) %><br>
  <% end %>
  <label>商品名</label><br>
  ¥<%= item.price %>
</div>
<% end %>